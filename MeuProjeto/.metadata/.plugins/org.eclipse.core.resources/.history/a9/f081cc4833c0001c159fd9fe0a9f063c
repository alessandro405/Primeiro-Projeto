package br.com.devjunior.servlets;

import java.io.IOException;
import java.sql.ResultSet;
import java.sql.SQLException;

import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import impl.UsuarioRepositorioJDBC;

@WebServlet(urlPatterns = {"/teste"})
public class Teste  extends HttpServlet {
	/**
	 * 
	 */
	private static final long serialVersionUID = -2706311068111453632L;
	
	
	@Override
	protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException {
		UsuarioRepositorioJDBC user = new UsuarioRepositorioJDBC();
		try {
			ResultSet resul = user.autenticar();
			
		} catch (IOException | SQLException e) {
			// TODO Auto-generated catch block
			e.printStackTrace();
		}

}

	/*
	 * @Override protected void doGet(HttpServletRequest req, HttpServletResponse
	 * resp) throws ServletException, IOException { UsuarioRepositorioJDBC
	 * agendaRepositorio = new UsuarioRepositorioJDBC(); try { List<Usuario> user =
	 * agendaRepositorio.listar(); req.setAttribute("listaUsuarios", user); } catch
	 * (SQLException e) { req.setAttribute("mensagemErro", e.getMessage()); } Object
	 * mensagemErro = req.getSession().getAttribute("mensagemErro"); if(mensagemErro
	 * != null) { req.setAttribute("mensagemErro", mensagemErro.toString());
	 * req.getSession().removeAttribute("mensagemErro"); } RequestDispatcher
	 * dispatcher =
	 * req.getServletContext().getRequestDispatcher("/WEB-INF/paginas/Teste.jsp");
	 * dispatcher.forward(req, resp);
	 * 
	 * }
	 */
}